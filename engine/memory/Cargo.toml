[package]
name = "memory"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

# Phase 1D: memory categories, budgeted accounting and a counting allocator for verification.
# No external dependencies.
[dependencies]
