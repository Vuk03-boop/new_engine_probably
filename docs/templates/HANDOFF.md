# NOW — <one current task or waiting state>

Updated:
Current authorization:
Active project root / baseline:

## What exists now

Brief factual implementation/documentation state. Separate approved design, source present, checks passed and pending acceptance.

## Last completed work

- Changed paths and purpose:
- Actual checks and evidence links:
- Checks NOT RUN / known failures:

## Boundaries and local state

- Files/decisions that must not be modified:
- User changes to preserve:
- Live processes, ports, pending jobs or generated artifacts:
- Git state if checked; otherwise explicitly unknown/absent:

## Blockers / unresolved questions

Only current consequential blockers, not the whole research backlog.

## Exact next action

One bounded action, its prerequisite/authorization and stop condition. Do not turn an old roadmap into an automatic task queue.

## Read next

Only the specific decision/change/source files needed for that action.

---
Use these fields to replace stale sections in `docs/NOW.md` at session end; do not create another competing handoff. Detailed closed results belong in their change record. Write the final user summary after this update; do not silently start a new task.
